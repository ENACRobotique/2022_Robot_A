#include "Arduino.h"
#include "test_base_moving.h"

namespace testLauncher {
    void launchTest(int index) {
        switch (index)
        {
        case 1:
            /* code */
            break;
        
        default:
            break;
        }
    }

}